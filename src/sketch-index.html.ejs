<!DOCTYPE html>
<html>
 <head>
  <style type="text/css">
    html, body { margin: 0; padding: 0; }
    body { background: #000; color: #eee; overflow: hidden; }
    #game { position: absolute; width: 640px; height: 480px; z-index: -999; }
    section { margin: 1em; }
    a { color: #fff; text-decoration: none }
    a:hover { text-decoration: underline; }
  </style>
 </head>
 <body>
  <section>
    <h1>Panic Ranger - Sketches</h1>
    <ul>
      <% htmlWebpackPlugin.options.sketches.forEach(name => { %>
        <li><a href="./sketches/<%= name %>"><%= name %></a></li>
      <% }) %>
    </ul>
  </section>
  <section>
    <div id="game"></div>
  </section>
 </body>
</html>
